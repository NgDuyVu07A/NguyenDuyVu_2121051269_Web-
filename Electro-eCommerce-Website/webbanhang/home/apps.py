from django.apps import AppConfig

class HomeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
    verbose_name = 'QUẢN LÝ CỬA HÀNG ELECTRO'  # <-- Thay chữ HOME cục mịch bằng dòng này